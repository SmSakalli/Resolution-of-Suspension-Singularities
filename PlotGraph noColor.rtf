{\rtf1\ansi\ansicpg1252\cocoartf2639
\cocoatextscaling0\cocoaplatform0{\fonttbl\f0\fswiss\fcharset0 Helvetica;}
{\colortbl;\red255\green255\blue255;}
{\*\expandedcolortbl;;}
\margl1440\margr1440\vieww11520\viewh8400\viewkind0
\pard\tx720\tx1440\tx2160\tx2880\tx3600\tx4320\tx5040\tx5760\tx6480\tx7200\tx7920\tx8640\pardirnatural\partightenfactor0

\f0\fs24 \cf0 # Paste the lines printed by Magma above this line\
\
# _________________________ PLOTTING CODE ____________________________________\
\
import matplotlib.pyplot as plt\
\
G = Graph(multiedges=True, loops=True)\
\
# Add vertices explicitly (needed when there are no edges)\
G.add_vertices(vertex_mult.keys())\
\
# Add edges\
G.add_edges(edge_list)\
\
# Original layout\
if G.num_verts() == 1:\
    pos = \{0: (0,0)\}\
else:\
    pos = G.layout('spring')\
    pos = \{v: (pos[v][0]*0.5, pos[v][1]*0.5) for v in pos\}\
\
fig, ax = plt.subplots(figsize=(10, 10))\
\
# Draw edges\
for u, v in G.edges(labels=False):\
    x1, y1 = pos[u]\
    x2, y2 = pos[v]\
    ax.plot([x1, x2], [y1, y2],\
            color='black',\
            linewidth=1.5,\
            zorder=1)\
\
# Draw vertices\
for v in G.vertices():\
    x, y = pos[v]\
\
    # Arrow vertices are hexagons\
    if v in arrow_vertices:\
        marker = (6, 0, 0)\
\
    # Nonzero-genus vertices are diamonds\
    elif vertex_genus[v] != 0:\
        marker = 'D'\
\
    # Ordinary vertices are circles\
    else:\
        marker = 'o'\
\
    ax.plot(x, y,\
            marker=marker,\
            markersize=40,\
            color='white',\
            markeredgecolor='black',\
            markeredgewidth=1.5,\
            zorder=2)\
\
    # Vertex labels\
    ax.annotate(\
        "%s\\n[%s], (%s)\\n(g:%s)" %\
        (v+1, vertex_mult[v], vertex_selfi[v], vertex_genus[v]),\
        xy=(x, y),\
        ha='center',\
        va='center',\
        fontsize=8,\
        zorder=3\
    )\
\
ax.axis('off')\
plt.tight_layout()\
plt.show()}